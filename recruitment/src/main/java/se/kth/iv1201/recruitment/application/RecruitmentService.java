/**
 * Handles the main recruitment flows (submit/update an application, recruiter actions later on).
 *
 * Entire use case should be wrapped in @Transactional so they're atomic (one flow, one outcome). 
 * Don't forget logging around important operations (HGP 9).
 *
 */


package se.kth.iv1201.recruitment.application;

public class RecruitmentService {

}
